language: node_js
deploy:
  provider: elasticbeanstalk
  access_key_id: $beanstalk_access_key_id
  secret_access_key: $beanstalk_secret_key
  region: us-east-1
  app: auth-app
  env: AuthApp-env
  on:
    repo: AlldredgeCraigSeniorProjectTeam/auth-app
